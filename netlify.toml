[build]
  command = "yarn generate"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "22"
  YARN_VERSION = "1.22.22"

# Security headers for all responses
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "accelerometer=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), payment=(), usb=()"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"
    # X-XSS-Protection removed: deprecated; CSP is primary XSS defense
    # Enforcing CSP; script-src includes 'unsafe-inline' for Nuxt/Vue hydration compatibility
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; connect-src 'self' https://api.frankfurter.dev; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'"

# Share page: no-referrer so r= param is never sent as referrer on outbound requests
[[headers]]
  for = "/share"
  [headers.values]
    Referrer-Policy = "no-referrer"

# Long-term cache for hashed assets
[[headers]]
  for = "/_nuxt/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
